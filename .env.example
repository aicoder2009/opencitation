# OpenCitation Environment Variables
# Copy this file to .env.local and fill in your values

# ============ CLERK AUTHENTICATION ============
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# ============ AWS DYNAMODB ============
# Required for production database
# If not set, the app uses an in-memory store (data lost on restart)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=your-secret-key
DYNAMODB_TABLE_NAME=opencitation

# Set to "true" to force local in-memory store even with AWS configured
# USE_LOCAL_DB=true

# ============ OPTIONAL ============
# OPENAI_API_KEY=sk-...  # For AI-powered citation extraction (fallback)
